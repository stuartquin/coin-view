import React from "react";

import "./Spinner.css";

const Spinner = () => {
  return (
    <div className="Spinner">
      <div className="Spinner--icon" />
    </div>
  );
};

export default Spinner;
